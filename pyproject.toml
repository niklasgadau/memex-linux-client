[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "memex-client"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
    "httpx>=0.27",
]

[project.scripts]
memex-client = "memex_client.cli:main"
